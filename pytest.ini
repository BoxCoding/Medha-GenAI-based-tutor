[pytest]
testpaths = tests
filterwarnings =
    # Upstream starlette/httpx transition notice (raised at import time by
    # fastapi.testclient) — not actionable here.
    ignore:Using `httpx` with `starlette.testclient` is deprecated.*
